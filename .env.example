# Laravel API base URL (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Toggle to run the app entirely against local mock data
# Set to "false" to call the live Laravel backend
NEXT_PUBLIC_USE_MOCKS=true

# Brand name (shown in meta tags, emails)
NEXT_PUBLIC_SITE_NAME=Bacha Stylo Fashion Home
